<div class="span-26 last">

<% unless @uncategorized.empty? %>

<h2>Uncategorized</h2>
<p class="unfinishedSlogan">These addons have not yet been tagged with a category in our database, but are still worth checking out!</p> 
  <% @uncategorized.each_with_index do |repo, index| %>
    <%= @repo = repo; @index = index; erb :repo %>
    <% if (index+1).divmod(3)[1] == 0 %><br style="clear:both;" /><% end %>
  <% end %>

<% end %>
</div>

<div class="span-23 last" style="clear:both;">
<% unless @incomplete.empty? %>

<h2 style="border-top: 4px solid #E6E8EA;">Not an addon</h2>
<p class="unfinishedSlogan">These repositories don't match the addon format described <a href="/howto/">here</a>.</p> 
  <% @incomplete.each_with_index do |repo, index| %>
    <%= @repo = repo; @index = index; erb :repo %>
  <% if (index+1).divmod(3)[1] == 0 %><br style="clear:both;" /><% end %>
  <% end %>

<% end %>
</div>


